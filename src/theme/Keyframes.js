import styled, { keyframes } from 'styled-components';

export const keyFrameExampleOne = keyframes`
    0% { opacity: 0; }
    100% { opacity: 1;}
}`;

export const portadaScale = keyframes`
  from {
    transform: scale(1);

  }
  to {

    transform: scale(1.2)

  }
  `;
